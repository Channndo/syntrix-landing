/** Feature flags for the static site (edit here; not overwritten by Netlify build). */
window.SYNTRIX_AUTH_ENABLED = true;
/** Set true to call SYNTRIX_API_BASE directly (requires API CORS to allow this site’s origin). */
// window.SYNTRIX_DISABLE_LOCAL_API_PROXY = true;
