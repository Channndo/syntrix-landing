# Builds auth-config.js from Auth0 secrets in Netlify → Site settings → Environment variables.
# Set: AUTH0_DOMAIN, AUTH0_CLIENT_ID, AUTH0_AUDIENCE (recommended, match Render API AUTH0_AUDIENCE)
[build]
  command = "node scripts/write-auth-config.mjs"
  publish = "."
